package model.world;

public class AntiHero extends Champion{
    public AntiHero(String name, int maxHP, int mana, int maxActionPointsPerTurn, int attackRange, int attackDamage, int speed) {
        super(name, maxHP, mana, maxActionPointsPerTurn, attackRange, attackDamage, speed);
    }
}
